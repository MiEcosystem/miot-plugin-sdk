declare namespace _default {
    /**
       * 从native的插件AppConfig中获取对应值
       * @since 10046
       * @method
       * @param key - 在appconfig js mapper中需要获取对应值的key
       * @param defalutValue - 如果没找到对应值，或appconfig请求出现错误，返回defaultvalue
       */
    function pluginAppConfigValueForKey(key: any, defalutValue: any): any;
    /**
       * 从native的插件AppConfig中获取对应值
       * @since 10046
       * @method
       * @param key - 在appconfig js mapper中需要获取对应值的key
       * @param defalutValue - 如果没找到对应值，或appconfig请求出现错误，返回defaultvalue
       */
    function pluginAppConfigValueForKey(key: any, defalutValue: any): any;
}
export default _default;